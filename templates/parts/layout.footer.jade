footer.main-footer
  small!=copy
    span Version&nbsp;
    span=$version."-".$commit
    :php
      $dev = (PHP_SAPI == 'cli-server');
    if dev
      span (DEV)
  .pull-right.hidden-xs
    small 
      Now Online&nbsp;&nbsp;
      span#otherUserContainer